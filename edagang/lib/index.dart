import 'package:edagang/main.dart';
import 'package:flutter/material.dart';


class Fintool extends StatelessWidget {

  @override
  Widget build(context) {
    return Scaffold(
        body: NewHomePage(0),
    );
  }
}

class Cartsini extends StatelessWidget {

  @override
  Widget build(context) {
    return Scaffold(
      body: NewHomePage(1),
    );
  }
}

class Index extends StatelessWidget {

  @override
  Widget build(context) {
    return Scaffold(
      body: NewHomePage(2),
    );
  }
}

class Goilmu extends StatelessWidget {

  @override
  Widget build(context) {
    return Scaffold(
      body: NewHomePage(3),
    );
  }
}

class Blurb extends StatelessWidget {

  @override
  Widget build(context) {
    return Scaffold(
      body: NewHomePage(4),
    );
  }
}